# Tuplas - Diccionarios.

settings = ('localhost', 3306, 'root', 'password', 'database')

host = settings[0]
port = settings[1]
username = settings[2]
password = settings[3]
name = settings[4]

print(host, port, username, password, name)
